{
  "name": "read-props-bar",
  "version": "1.0.0",
  "description": "Reading-view properties bar with PPT-style deck navigation for Obsidian: one `deck` property drives prev/next and auto-computed page numbers.",
  "main": "main.js",
  "scripts": {
    "build": "esbuild main.ts --bundle --format=cjs --external:obsidian --outfile=main.js --sourcemap=inline",
    "check": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "obsidian": "^1.7.2",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9"
  }
}
